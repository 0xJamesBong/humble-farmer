# Optional: Binance API keys for FreqTrade live trading (also configurable in freqtrade/user_data/config.json).
# Copy to repo root .env if you use tooling that reads env vars. Do not commit .env.
BINANCE_API_KEY=
BINANCE_API_SECRET=
